// use std::ops::{Add, Mul, Index, IndexMut};
// use std::fmt::Display;
// use std::fmt::Debug;
//
// pub trait TensorTrait<f64>: Clone + Display + Add<f64, Output = f64> + Mul<f64, Output = f64> + Index + IndexMut { }
// impl TensorTrait<f64> for f64 where f64: Clone + Display + Add<f64, Output = f64> + Mul<f64, Output = f64> + Index + IndexMut { }
